import multer from"multer";import{extname,resolve}from"path";const aleatorio=()=>Math.floor();export default{storage:multer.diskStorage({destination:(e,o,t)=>{t(null,resolve(__dirname,"..","..","uploads"))},filename:(e,o,t)=>{t(null,`${Date.now()}${extname(o.originalname)}`)}})};